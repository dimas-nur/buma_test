name: home
description: Feature Home.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.2.6 <4.0.0"
  flutter: ">=1.17.0"

# Local Dependencies
shared_dependencies:
  path: ../../packages/shared_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Inherited DEV Dependencies
  flutter_lints:

flutter:
  uses-material-design: true
